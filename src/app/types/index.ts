export type Template = {
  id: string;
  title: string;
  image: string;
  category: string;
  shortDescription: string; // for Card
  longDescription: string;  // for Modal (rich article-like HTML)
};
